# Task 6: File Encryption

Implement file encryption/decryption:
- encrypt_file(input_path, output_path, key)
- decrypt_file(input_path, output_path, key)
- Use AES-256 (cryptography library)
- Handle large files in chunks
- Include key derivation from password (PBKDF2)

Save to: `tests/programming-test-v2/solutions/encryption.py`
