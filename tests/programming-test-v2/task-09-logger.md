# Task 9: Structured Logger

Create a structured logging utility:
- JSON-formatted log output
- Log levels (DEBUG, INFO, WARNING, ERROR, CRITICAL)
- Context fields (request_id, user_id)
- File and stdout handlers
- Log rotation support

Save to: `tests/programming-test-v2/solutions/structured_logger.py`
