# Task 4: SQLAlchemy CRUD

Create a simple ORM model and CRUD operations:
- User model (id, name, email, created_at)
- create_user, get_user, update_user, delete_user, list_users
- Use SQLAlchemy with SQLite
- Include connection context manager

Save to: `tests/programming-test-v2/solutions/sqlalchemy_crud.py`
