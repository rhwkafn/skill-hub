# Task 5: Email Sender

Create an email sending utility:
- send_email(to, subject, body, smtp_config)
- Support HTML and plain text
- Support attachments
- Use smtplib + email.mime
- Include retry on connection failure

Save to: `tests/programming-test-v2/solutions/email_sender.py`
