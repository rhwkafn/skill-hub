# Task 8: Rate Limiter

Implement a rate limiter:
- @rate_limit(calls=10, period=60) decorator
- Token bucket algorithm
- Thread-safe
- Returns wait time when limit exceeded
- Support for both sync and async functions

Save to: `tests/programming-test-v2/solutions/rate_limiter.py`
