# Task 2: Data Processing Pipeline

Create a Python script that:
- Reads a CSV file into pandas DataFrame
- Cleans missing values (fill numeric with median, categorical with mode)
- Adds computed columns (e.g., profit_margin = profit/revenue)
- Groups by category and computes aggregates
- Exports cleaned data to new CSV

Save to: `tests/programming-test-v2/solutions/data_pipeline.py`
