# Task 7: JSON Schema Validator

Create a JSON schema validator that:
- Validates data against a schema definition
- Supports types: string, number, boolean, array, object
- Supports required fields, min/max, pattern (regex)
- Returns list of validation errors
- No external dependencies

Save to: `tests/programming-test-v2/solutions/schema_validator.py`
