# Task 10: Config Parser

Build a config parser that:
- Reads YAML and TOML config files
- Supports environment variable substitution (${VAR} syntax)
- Merges multiple config sources (file + env + defaults)
- Validates required fields
- Returns typed config object

Save to: `tests/programming-test-v2/solutions/config_parser.py`
