# Task 1: Binary Search Implementation

Write a Python function `binary_search(arr, target)` that:
- Takes a sorted list and a target value
- Returns the index if found, -1 if not
- Include type hints and docstring
- Handle edge cases (empty list, single element)

Save to: `tests/programing-test/solutions/binary_search.py`
