# Task 7: CSV Parser

Write a CSV parser function that:
- Reads CSV with headers
- Returns list of dicts
- Handles quoted fields with commas
- Handles missing values (None)
- No external dependencies

Save to: `tests/programing-test/solutions/csv_parser.py`
