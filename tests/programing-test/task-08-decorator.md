# Task 8: Caching Decorator

Create a `@cache` decorator that:
- Caches function results based on arguments
- Has a max_size parameter (LRU eviction)
- Shows cache hit/miss stats
- Works with any hashable arguments

Save to: `tests/programing-test/solutions/cache_decorator.py`
